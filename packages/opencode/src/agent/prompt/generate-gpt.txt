GPT generation compatibility rules:

- The generated agent will run with the tools exposed by its selected model. Treat that runtime tool list as the only source of truth; do not mention or require legacy tool names that may be absent.
- For delegation examples, use the `actor` tool and its `subagent_type` field. Do not write `Agent tool`, `Agent`, or another invented tool name.
- For Codex-harness agents, describe file inspection through `exec` using `tools.exec_command({cmd: "..."})` with targeted `rg`, `rg --files`, and `sed -n` commands. Describe edits through `tools.apply_patch` and visual inspection through `tools.view_image` inside `exec`. Authorized main and registered peer actors may use the full declared `tools.actor({operation: ...})` inside `exec`, including existing resume without widening recovery eligibility or task binding. Ordinary subagents remain restricted to authorized sends to their real registered parent. `question` can run inside `exec` when interactive and authorized; `plan_exit` is an exclusive terminal operation for a foreground main plan turn: await other calls first, approval commits the build continuation and stops all further script execution while switching the TUI, and decline or feedback lets the script continue. The next turn rebuilds build permissions. Direct `actor`, `question`, and `plan_exit` remain available when listed; session and workflow controls stay direct. Task and skill operations use the declared nested methods. Nested calls retain request permissions, frozen tool membership, and disabled-tool guards.
- Keep generated instructions model-agnostic where possible. Do not claim that `Glob`, `Grep`, `Read`, `Write`, or `Bash` are available unless the request or current tool schema explicitly establishes those names.
- Examples must be internally consistent: every tool referenced in an example must be available to the agent in that example, and delegation must be represented as an `actor` tool call.
