# 2026-09-26 upstream synchronization

## Selected range and capability inventory (6)

Audit range: upstream `2b993ac98283bb1283313982e699df706e9ad235..849ca66cc8debbbcc08904039e05bd2e79be348d` (six first-parent commits, 55 paths); starting fork `main=d1d42cfac9a9d530b86f4877c0e4433be02f569d`, `dev/compat=b4885f356caaf14e4601ef400887e07733c95f91`. Main runtime merge: `202de026e2eee4a728f0cd881e5417df45acdc4c`; CI follow-up behavior and generated types: `085f149a29b09fd6e3ef23ebd62e4ae2c755d925`. The incoming range changes no dependency manifest, lockfile, migration or workflow. Upstream `git diff --check` is clean.

| ID | Upstream content | main merge result | dev/compat merge result | Key paths and evidence |
| --- | --- | --- | --- | --- |
| C01 | `37c3e1ef`: remove the unreleased same-step duplicate tool-call cancellation. | Adopt removal; keep FC-001 FIFO admission, failure cascade and request replay boundary. | Inherit; keep frozen tool membership and DC-CONTEXT-001 preflight. | `session/{prompt,processor,toolcall-duplicate}.ts`, flag and former duplicate tests. Counterparts: shared prompt/processor, compat prompt/processor. Relationship: partial duplicate removed; drift: behavior, tests, docs. Owner: shared `main`; disposition: adopt. |
| C02 | `1f5630c8`: await an `Instance.provide` fixture before its temporary Git directory is disposed. | Adopt fixture lifetime correction. | Inherit fixture correction. | `test/server/session-recovery.test.ts`. Counterparts: shared fixture and compat recovery tests. Relationship: equivalent fixture repair; drift: tests. Owner: shared `main`; disposition: adopt. |
| C03 | `fe394f3d`: make interactive asks independent of background status for agent-spawned subagents; require an explicit `computer` approval and isolate rejection by source. | Adopt interactive routing and forced ask; retain FD-001 deny precedence, run invocation scope and inherited parent grants. | Inherit; preserve full-context actor permission snapshots. | `agent/config.ts`, `permission/index.ts`, `session/prompt.ts`, TUI approval and tests. Counterparts: shared approval/Actor, compat frozen permission capture. Relationship: complementary; drift: behavior, tests. Owner: shared `main`; disposition: adapt. |
| C04 | `e68899b3`: remove TaskGate completion re-entry and task-based passive status downgrade. | Adopt removal; preserve task auto-start, model Status/Summary header, terminal claim and single notification. | Inherit; preserve DC-ACTOR-001 full-context and DC-ACTOR-002 variant transport. | `actor/{spawn,waiter}.ts`, `tool/{actor,task}.ts`, former task gate and actor tests. Counterparts: shared generation lifecycle, compat spawn/variant. Relationship: conflicting gate removed, complementary lifecycle retained; drift: behavior, tests, docs. Owner: shared `main`; disposition: adapt. |
| C05 | `0968f56a`: add host-defined API error catalog, trusted binding, error normalization and terminal/persistent/bounded retry classes. | Adopt registry and live provenance; preserve FD-005 model identity, FC-013 configured request and candidate/judge budgets, MaxMode retry-event ownership and replay guard. | Inherit registry with compat model and request preflight preserved. | `error/host-registry.ts`, `provider/error.ts`, `session/{llm,message-v2,retry,max-mode}.ts`, generated SDK types, shared error schema and tests. Counterparts: shared retry and model selection, compat LLM/request/max-mode. Relationship: complementary with scoped-budget conflict reconciled; drift: behavior, schema/config, tests, docs. Owner: shared `main`; disposition: adapt. |
| C06 | `849ca66c`: close disposed event streams and prevent an unauthorized retry after a bound host error. | Adopt stream cleanup and terminal retry guard. | Inherit with compat session lifecycle intact. | `server/routes/instance/event.ts`, `session/retry.ts`, bus and retry tests. Counterparts: shared event/permission state, compat session lifecycle. Relationship: complementary; drift: behavior, tests. Owner: shared `main`; disposition: adopt. |

Inventory count: 6 capabilities, 6 result rows. Each row has a canonical owner and disposition. Active FD/FC/DC surfaces were checked against every changed production, fixture and documentation path, including clean-merging files. No capability requires a new compat-only owner. The TaskGate-era cancellation test was removed because its second model request no longer exists; the remaining cancellation and notification tests cover the retained lifecycle.

## Main validation

The default-path tests unset ambient `MIMOCODE_EXPERIMENTAL`, `MIMOCODE_EXPERIMENTAL_MCP_TOOL_SEARCH` and `MIMOCODE_CODEX_MODE`; package preload flags remain unchanged. `bun typecheck` passed. Root `bun lint` reported 4670 warnings and zero errors. `git diff --check` passed. The stable host/error/retry/LLM/bus matrix passed 239/239 tests; the stable Actor/approval/TUI/recovery matrix passed 174/174. No dependency install was needed; `bun.lock` is unchanged.

The first exact-tip CI exposed five missed intersections: MaxMode's inner LLM retry consumed candidate retries without emitting MaxMode status, a background subagent now asks interactively, permission reject cascades only within one source, and the retry-density fixture assumed persistent request retries. PR #150 fixes the runtime owner and aligns fixtures with the retained fork budgets; its focused matrix passed 15/15 and the host/error/retry/LLM/bus matrix passed 239/239. SDK regeneration then added host error and retry-event fields to `types.gen.ts`; a UI error-message reader now narrows the generated union. The second SDK generation produced no further diff and OpenAPI reference tests passed 4/4. Two PR CI runs then timed out at the same plugin-backed toolset test: the server plugin loader waited for unrelated config-directory dependency installation before loading an external file plugin. Server plugins now load first and wait/retry only when a file plugin inside an installation directory fails. The plugin-loader/toolset matrix passes 38/38; package typecheck and root lint pass with zero errors. The final PR SHA still requires exact-SHA CI before acceptance.

## Publication gates

Completion additionally requires remote-tip equality, ancestry from the selected upstream SHA through fork `main` to `dev/compat`, and successful lint/typecheck/test CI for the exact final SHAs. The final branch-tip evidence is reported with the sync result; historical CI is not evidence for new tips.
